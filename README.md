# Public resume

Drop the general resume here (e.g. `resume.pdf`, or an `index.html` for the Pages site).
Anything in this folder gets synced to `shubhampaithankar/shubhampaithankar.github.io` on push to `main`.
